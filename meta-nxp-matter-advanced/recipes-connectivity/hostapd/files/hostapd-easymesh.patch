Upstream-Status: Inappropriate  [platform specific changes]

diff --git a/hostapd/Makefile b/hostapd/Makefile
index 3bce295..f7dda4a 100644
--- a/hostapd/Makefile
+++ b/hostapd/Makefile
@@ -1302,6 +1302,10 @@ ifdef CONFIG_NO_TKIP
 CFLAGS += -DCONFIG_NO_TKIP
 endif
 
+ifdef CONFIG_NXP_EASYMESH
+CFLAGS += -DCONFIG_NXP_EASYMESH
+endif
+
 $(DESTDIR)$(BINDIR)/%: %
 	install -D $(<) $(@)
 
diff --git a/src/ap/ap_config.c b/src/ap/ap_config.c
index 298ba67..2b46236 100644
--- a/src/ap/ap_config.c
+++ b/src/ap/ap_config.c
@@ -183,6 +183,10 @@ void hostapd_config_defaults_bss(struct hostapd_bss_config *bss)
 	bss->uap_pre_script = NULL;
 	bss->uap_post_script = NULL;
 #endif /* CONFIG_UAP_EXTRA_SCRIPT */
+
+#ifdef CONFIG_NXP_EASYMESH
+	bss->nxp_easymesh = 0;
+#endif
 }
 
 
diff --git a/src/ap/ap_config.h b/src/ap/ap_config.h
index d236df8..7813077 100644
--- a/src/ap/ap_config.h
+++ b/src/ap/ap_config.h
@@ -1000,6 +1000,13 @@ struct hostapd_bss_config {
 	 */
 	char *uap_post_script;
 #endif /* CONFIG_UAP_EXTRA_SCRIPT */
+
+#ifdef CONFIG_NXP_EASYMESH
+	/**
+	 * NXP EasyMesh mode: 0=disabled, 1=enabled
+	 */
+	int nxp_easymesh;
+#endif
 };
 
 /**
diff --git a/src/drivers/driver_nl80211.c b/src/drivers/driver_nl80211.c
index a778b91..eb6e53f 100644
--- a/src/drivers/driver_nl80211.c
+++ b/src/drivers/driver_nl80211.c
@@ -2477,6 +2477,12 @@ static int nl80211_alloc_mgmt_handle(struct i802_bss *bss)
 	return 0;
 }
 
+#ifdef CONFIG_NXP_EASYMESH
+static int nl80211_nxp_easymesh_enabled(struct i802_bss *bss)
+{
+	return bss && bss->drv ? bss->drv->nxp_easymesh : 0;
+}
+#endif
 
 static void nl80211_mgmt_handle_register_eloop(struct i802_bss *bss)
 {
@@ -2537,18 +2543,24 @@ static int nl80211_mgmt_subscribe_non_ap(struct i802_bss *bss)
 		ret = -1;
 #endif /* CONFIG_INTERWORKING */
 #if defined(CONFIG_P2P) || defined(CONFIG_INTERWORKING) || defined(CONFIG_DPP)
-	/* GAS Initial Request */
-	if (nl80211_register_action_frame(bss, (u8 *) "\x04\x0a", 2) < 0)
-		ret = -1;
-	/* GAS Initial Response */
-	if (nl80211_register_action_frame(bss, (u8 *) "\x04\x0b", 2) < 0)
-		ret = -1;
-	/* GAS Comeback Request */
-	if (nl80211_register_action_frame(bss, (u8 *) "\x04\x0c", 2) < 0)
-		ret = -1;
-	/* GAS Comeback Response */
-	if (nl80211_register_action_frame(bss, (u8 *) "\x04\x0d", 2) < 0)
-		ret = -1;
+#ifdef CONFIG_NXP_EASYMESH
+	if (!nl80211_nxp_easymesh_enabled(bss)) {
+#endif
+		/* GAS Initial Request */
+		if (nl80211_register_action_frame(bss, (u8 *) "\x04\x0a", 2) < 0)
+			ret = -1;
+		/* GAS Initial Response */
+		if (nl80211_register_action_frame(bss, (u8 *) "\x04\x0b", 2) < 0)
+			ret = -1;
+		/* GAS Comeback Request */
+		if (nl80211_register_action_frame(bss, (u8 *) "\x04\x0c", 2) < 0)
+			ret = -1;
+		/* GAS Comeback Response */
+		if (nl80211_register_action_frame(bss, (u8 *) "\x04\x0d", 2) < 0)
+			ret = -1;
+#ifdef CONFIG_NXP_EASYMESH
+	}
+#endif
 	/* Protected GAS Initial Request */
 	if (nl80211_register_action_frame(bss, (u8 *) "\x09\x0a", 2) < 0)
 		ret = -1;
@@ -2586,11 +2598,17 @@ static int nl80211_mgmt_subscribe_non_ap(struct i802_bss *bss)
 #undef NAN_PUB_ACTION
 #endif /* CONFIG_NAN_USD */
 #ifdef CONFIG_DPP
-	/* DPP Public Action */
-	if (nl80211_register_action_frame(bss,
-					  (u8 *) "\x04\x09\x50\x6f\x9a\x1a",
-					  6) < 0)
-		ret = -1;
+#ifdef CONFIG_NXP_EASYMESH
+        if (!nl80211_nxp_easymesh_enabled(bss)) {
+#endif
+		/* DPP Public Action */
+		if (nl80211_register_action_frame(bss,
+						  (u8 *) "\x04\x09\x50\x6f\x9a\x1a",
+						  6) < 0)
+			ret = -1;
+#ifdef CONFIG_NXP_EASYMESH
+        }
+#endif
 #endif /* CONFIG_DPP */
 #ifdef CONFIG_OCV
 	/* SA Query Request */
@@ -2744,12 +2762,18 @@ static int nl80211_action_subscribe_ap(struct i802_bss *bss)
 {
 	int ret = 0;
 
-	/* Public Action frames */
-	if (nl80211_register_action_frame(bss, (u8 *) "\x04", 1) < 0)
-		ret = -1;
-	/* RRM Measurement Report */
-	if (nl80211_register_action_frame(bss, (u8 *) "\x05\x01", 2) < 0)
-		ret = -1;
+#ifdef CONFIG_NXP_EASYMESH
+	if (!nl80211_nxp_easymesh_enabled(bss)) {
+#endif
+		/* Public Action frames */
+		if (nl80211_register_action_frame(bss, (u8 *) "\x04", 1) < 0)
+			ret = -1;
+		/* RRM Measurement Report */
+		if (nl80211_register_action_frame(bss, (u8 *) "\x05\x01", 2) < 0)
+			ret = -1;
+#ifdef CONFIG_NXP_EASYMESH
+	}
+#endif
 	/* RRM Link Measurement Report */
 	if (nl80211_register_action_frame(bss, (u8 *) "\x05\x03", 2) < 0)
 		ret = -1;
@@ -2765,9 +2789,15 @@ static int nl80211_action_subscribe_ap(struct i802_bss *bss)
 	/* Protected Dual of Public Action */
 	if (nl80211_register_action_frame(bss, (u8 *) "\x09", 1) < 0)
 		ret = -1;
-	/* WNM */
-	if (nl80211_register_action_frame(bss, (u8 *) "\x0a", 1) < 0)
-		ret = -1;
+#ifdef CONFIG_NXP_EASYMESH
+	if (!nl80211_nxp_easymesh_enabled(bss)) {
+#endif
+		/* WNM */
+		if (nl80211_register_action_frame(bss, (u8 *) "\x0a", 1) < 0)
+			ret = -1;
+#ifdef CONFIG_NXP_EASYMESH
+	}
+#endif
 	/* WMM */
 	if (nl80211_register_action_frame(bss, (u8 *) "\x11", 1) < 0)
 		ret = -1;
@@ -2987,6 +3017,24 @@ wpa_driver_nl80211_finish_drv_init(struct wpa_driver_nl80211_data *drv,
 	int send_rfkill_event = 0;
 	enum nl80211_iftype nlmode;
 
+#ifdef CONFIG_NXP_EASYMESH
+#ifdef HOSTAPD
+	drv->nxp_easymesh = 0;
+	if (driver_params) {
+		const char *pos = os_strstr(driver_params, "nxp_easymesh=");
+		if (pos)
+			drv->nxp_easymesh = atoi(pos + 13);
+	}
+#else
+	if (drv->ctx) {
+		/* wpa_supplicant path - get from config */
+		struct wpa_supplicant *wpa_s = (struct wpa_supplicant *)drv->ctx;
+		if (wpa_s->conf)
+			drv->nxp_easymesh = wpa_s->conf->nxp_easymesh;
+	}
+#endif
+#endif /* CONFIG_NXP_EASYMESH */
+
 	drv->ifindex = if_nametoindex(bss->ifname);
 	bss->ifindex = drv->ifindex;
 	bss->wdev_id = drv->global->if_add_wdevid;
diff --git a/src/drivers/driver_nl80211.h b/src/drivers/driver_nl80211.h
index 8b3d1d1..e19f686 100644
--- a/src/drivers/driver_nl80211.h
+++ b/src/drivers/driver_nl80211.h
@@ -275,6 +275,10 @@ struct wpa_driver_nl80211_data {
 	u8 *pending_link_reconfig_data;
 	size_t pending_link_reconfig_data_len;
 #endif /* CONFIG_DRIVER_NL80211_QCA */
+
+#ifdef CONFIG_NXP_EASYMESH
+	int nxp_easymesh;
+#endif
 };
 
 struct nl_msg;
